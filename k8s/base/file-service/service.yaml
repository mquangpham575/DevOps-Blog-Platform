apiVersion: v1
kind: Service
metadata:
  name: file-service
  namespace: blog-app
  labels:
    app: file-service
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
  selector:
    app: file-service
