apiVersion: v1
kind: Service
metadata:
  name: blog-db
  namespace: blog-app
  labels:
    app: blog-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: blog-db
