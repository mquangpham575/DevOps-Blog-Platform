apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: file-db-pvc
  namespace: blog-app
  labels:
    app: file-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
