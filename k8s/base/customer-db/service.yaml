apiVersion: v1
kind: Service
metadata:
  name: customer-db
  namespace: blog-app
  labels:
    app: customer-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: customer-db
