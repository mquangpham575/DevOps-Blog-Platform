apiVersion: v1
kind: Service
metadata:
  name: interaction-service
  namespace: blog-app
  labels:
    app: interaction-service
spec:
  type: ClusterIP
  ports:
    - port: 8086
      targetPort: 8086
  selector:
    app: interaction-service
