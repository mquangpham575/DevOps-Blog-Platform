apiVersion: v1
kind: Service
metadata:
  name: interaction-db
  namespace: blog-app
  labels:
    app: interaction-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: interaction-db
