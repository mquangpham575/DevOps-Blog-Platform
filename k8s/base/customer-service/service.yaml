apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: blog-app
  labels:
    app: customer-service
spec:
  type: ClusterIP
  ports:
    - port: 8087
      targetPort: 8087
  selector:
    app: customer-service
