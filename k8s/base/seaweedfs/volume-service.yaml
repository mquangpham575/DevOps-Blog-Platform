apiVersion: v1
kind: Service
metadata:
  name: seaweedfs-volume
  namespace: blog-app
  labels:
    app: seaweedfs-volume
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: seaweedfs-volume
