apiVersion: v1
kind: Service
metadata:
  name: seaweedfs-filer
  namespace: blog-app
  labels:
    app: seaweedfs-filer
spec:
  type: ClusterIP
  ports:
    - port: 8888
      targetPort: 8888
  selector:
    app: seaweedfs-filer
