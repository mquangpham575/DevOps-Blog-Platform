apiVersion: v1
kind: Service
metadata:
  name: seaweedfs-master
  namespace: blog-app
  labels:
    app: seaweedfs-master
spec:
  type: ClusterIP
  ports:
    - port: 9333
      targetPort: 9333
  selector:
    app: seaweedfs-master
