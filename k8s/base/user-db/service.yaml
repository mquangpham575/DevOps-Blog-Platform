apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: blog-app
  labels:
    app: user-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: user-db
